# Calunga UI Environment Configuration

# ============================================================================
# Application Settings
# ============================================================================
NODE_ENV=development
PORT=8080

# ============================================================================
# Authentication
# ============================================================================
# Whether RBAC authentication is required
AUTH_REQUIRED=false

# OIDC/Keycloak Configuration (optional)
# OIDC_CLIENT_ID=calunga-ui
# OIDC_SCOPE=openid profile email
# OIDC_SERVER_URL=http://localhost:8090
# OIDC_SERVER_IS_EMBEDDED=true
# OIDC_SERVER_EMBEDDED_PATH=/auth/realms/calunga

# ============================================================================
# API Configuration
# ============================================================================
# Main Calunga API endpoint
# Only needed if you have a Calunga backend server running
# Leave commented out if you're only using Pulp
# CALUNGA_API_URL=http://localhost:8080

# ============================================================================
# Pulp Configuration
# ============================================================================
# Pulp server URL (default Pulp port is 24817)
# For hosted Pulp instances, include the full URL with API path and domain
# Example: https://packages.redhat.com/api/pulp/calunga-ui-dev/
# Note: Domain is included in the URL path, not as a separate parameter
PULP_API_URL=http://localhost:24817

# Pulp service account credentials for API access
# Create a service account in Pulp with read permissions:
#   pulpcore-manager add-user <username> --password <password>
# For hosted instances, use the service account credentials or JWT token
# These are used for Basic Authentication (equivalent to curl -u username:password)
PULP_USERNAME=admin
PULP_PASSWORD=password

# Verify SSL certificates when connecting to Pulp (server-side only)
# Set to "false" for self-signed certificates or development
# Example: PULP_VERIFY_SSL=false
PULP_VERIFY_SSL=true

# ============================================================================
# UI Settings
# ============================================================================
# File upload size limit (MB)
UI_INGRESS_PROXY_BODY_SIZE=500m

# Mock data mode (off, stub, record)
MOCK=off

# Branding location (optional)
# BRANDING=/path/to/branding/files
